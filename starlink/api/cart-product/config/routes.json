{
  "routes": [
    {
      "method": "GET",
      "path": "/cart-products",
      "handler": "cart-product.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cart-products/count",
      "handler": "cart-product.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cart-products/:id",
      "handler": "cart-product.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/cart-products",
      "handler": "cart-product.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cart-products/:id",
      "handler": "cart-product.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cart-products/:id",
      "handler": "cart-product.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
